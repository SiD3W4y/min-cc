; Data defined at the beginning of the file

str hello_world "Hello World !"
num size 14

fn main
	mov $A #hello_world	; str address into A
	ldr $B #size		; str size into B
	sys			; syscall write

	mov $A 0x2		; syscall exit
	mov $B 0x0		; exit code
	sys			; syscall exit

