; Data defined at the beginning of the file

str hello_world "Hello World !"
num size 14

fn exit
	mov $A 0x2
	mov $B 0x0
	sys

fn main
	mov $A 0x00		; syscall write number 
	ldr $B #hello_world	; str address into A
	ldr $C #size		; str size into B
	sys			; syscall write

	jmp exit

